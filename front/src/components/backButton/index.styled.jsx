import styled from "@emotion/styled";

export const BackButtonStyled = styled.div`
  display: inline-flex;
  align-items: center;
  padding-top: 60px;
  padding-left: 20px;

  cursor: pointer;
  transition: opacity 0.7s;

  &:hover {
    opacity: 0.7;
  }
`;
